<li class="list-group-item">
<div class="row d-flex-inline">
	<div class="col-11">
		{{ reservation.date|date("d/m/Y") }}
		{{ reservation.time|date("H:i") }}
		-
		{{ reservation.nbClient }}
		client{% if reservation.nbClient > 1 %}s{% endif %}
		-
		{{ reservation.name }}
		({{ reservation.phone }})
	</div>
	<div class="col-1">
		{% if reservation.confirm == false %}<i class="fas fa-exclamation text-warning"></i>{% elseif reservation.confirm == true %}<i class="fas fa-check text-success"></i>{% endif %}
	</div>
</div>
	
</li>
